# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next
/.next/
/out/

# Vercel local link output (may contain project metadata)
/.vercel

# Secrets — NEVER commit real credentials
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
.env.test.local
# Template only (placeholders, no secrets)
!.env.example

# debug / local build captures
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
build-log.txt
build-output.txt
build-output*.txt

# typescript
next-env.d.ts
*.tsbuildinfo

# local evidence uploads (not for Vercel)
/storage

# test / coverage
/coverage

# OS
Thumbs.db
Desktop.ini
.DS_Store
